<rewrite>
  <rules>
    <rule name="Redirect to fixed URL" stopProcessing="true">
      <match url="^(.*)//+(.*)$" />
      <action type="Redirect" url="{R:1}/{R:2}" />
    </rule>
  </rules>
</rewrite>
